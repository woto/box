<!-- <p id="notice"><%= notice %></p> -->

<div class="mt-3">
  <%= render 'form', communicate: @communicate %>
</div>

<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>Message</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>

<!--
<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>#</th>
      <th>Message</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @communicates.each do |communicate| %>
    <tr>
      <th scope="row"><%= communicate.id %></th>
      <td><%= communicate.message %></td>
      <td><%= l communicate.created_at, format: :short %></td>
      <td><%= link_to 'Show', communicate %></td>
      <td><%= link_to 'Edit', edit_communicate_path(communicate) %></td>
      <td><%= link_to 'Destroy', communicate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>
-->
